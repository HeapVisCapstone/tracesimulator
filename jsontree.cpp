// #include <vector>

#include "jsontree.h"

// using boost::property_tree::ptree
// using std::vector

// template <class D>
// ptree ptreeOf(const JSONNode<D>& t, const std::function<ptree(D)>& writeData) {

//     ptree children;
//     for (const auto& c : t.getChildren()) {
//         children.put("", ptreeOf(c, writeData));
//     }
//     ptree node = writeData(t.getData());
//     ptree.put("children", children);

//     return ptree;
// }

